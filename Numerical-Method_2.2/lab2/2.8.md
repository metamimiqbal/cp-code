The exercise sheet does not provide enough information to generate a correct implementation of Ramanujan's method for all exercises.

Unlike the Bisection and Secant pages, these exercises only list the equations. Ramanujan's method requires the coefficients of the normalized series

[
f(x)=1-(a_1x+a_2x^2+a_3x^3+\cdots)=0,
]

and for several problems (especially 2, 3, and 4) those coefficients come from **infinite series expansions** (e.g. (e^x), (\cos x), etc.). The page does not specify:

* how many series terms (M) to use,
* the stopping tolerance,
* whether to truncate automatically,
* or the exact workflow used by your lab for expanding transcendental functions.

If I guessed those details, the outputs would likely differ from your lab manual.

Please upload the **next page(s)** of the Ramanujan experiment (the continuation after page 18), especially if it contains:

* the implementation details,
* the number of terms (M),
* the worked algorithm,
* or sample outputs.

With that page, I can produce a single Python file in exactly the same style as your previous Bisection and Secant programs (`ex1()`...`ex11()`, `main()`, identical table formatting, procedural code only).
