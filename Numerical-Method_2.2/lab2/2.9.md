I can't correctly generate the code for this experiment from this page alone.

The exercises explicitly instruct you to:

> "Rewrite each system as (x=F(x,y),; y=G(x,y)) **so that the convergence condition holds**"

That means the actual iteration functions are **part of the solution**, not uniquely determined by the equations.

For example, Exercise 2 is

[
x^2+y^2-4=0,\qquad xy-1=0
]

Possible valid rearrangements include

```text
x = sqrt(4-y^2)
y = 1/x
```

or

```text
x = 1/y
y = sqrt(4-x^2)
```

or

```text
x = (1+y)/...
```

Each produces different iteration tables, and only some satisfy the convergence criterion at the given starting point.

The same ambiguity exists for Exercises 3–10. The lab expects the student to choose a suitable rearrangement based on the partial derivatives.

So unlike Bisection and Secant, there is **no single algorithm** that can be inferred from the page.

If I invent the rearrangements, I'd be fabricating part of the assignment rather than reproducing the lab's intended workflow.

If you have the instructor's class notes, a solved example sheet, or even one completed exercise showing how they choose (F(x,y)) and (G(x,y)), I can then generate the complete Python program (`ex1()` ... `ex10()`, `main()`, iteration tables, identical formatting) in the same style as the previous experiments. Without those rearrangements, there isn't a unique correct implementation.
